import { NextPage, GetStaticProps } from "next";
import CinemaInfo from "screens/dashboard/cinema/CinemaInfo";

const MainCinemaPage: NextPage = () => {
  return <CinemaInfo />;
};

export default MainCinemaPage;
